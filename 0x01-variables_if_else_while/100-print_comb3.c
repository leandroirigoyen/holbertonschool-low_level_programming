#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - main
 * Return:


for (i = 0; i <= 7; i++)
{
    for (j = i+1; j <= 8; j++)
    {
        for (k = j+1; k <= 9; k++)
        {
            printf("%d%d%d\n", i, j, k);
        }
    }
}
