#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - main
 * Return: algo
 */
int main(void)
{
int x;
char sl = '\n';

for (x = 'a'; x <= 'z'; x++)
{
if (x == 'q' || x == 'e')
}

else
{
putchar(x);
}

putchar(sl);
return (0);
}
