#include <stdlib.h>
#include "_putchar.c"
#include <ctype.h>
/**
  * main - main
  * Return: 1 if is lowercase if it's not print 0
*/
int main(void)
{


/**
 * _islower - _islower
 * Return: 1
 */
int _islower(int c)


if (islower(c))
{
return (1);
}
else
{
return (0);
}

return (0);
}
