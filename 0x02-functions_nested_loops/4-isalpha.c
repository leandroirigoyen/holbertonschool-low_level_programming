#include "main.h"
#include <ctype.h>
/**
 * _isalpha - _isalpha
 * @c: letter, lowercase or uppercase
 * Return: 0
 */

int _isalpha(int c)
{

if (isalpha(c))
{
_putchar(c);
}
else
{
return (0);
}


return (0);
}
