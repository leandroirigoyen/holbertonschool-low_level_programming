#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - imprimir ultimo digito
 * @n: var
 * Return: value of the last digit
 */

int print_last_digit(int n)
{
_putchar(n);	
return (n % 10);
}
