#include "main.h"
#include <ctype.h>
/**
 * _isupper - check uppercase
 * Return: 1 if c is upper case, return 0 otherwise
 * @c: var
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}




return (0);
}
