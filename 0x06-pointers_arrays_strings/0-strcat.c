#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcat - cat
 * @dest: destination
 * @src: source
 * Return: 0
 */

char *_strcat(char *dest, char *src)
{

strcat(dest, src);

return (dest);
}
