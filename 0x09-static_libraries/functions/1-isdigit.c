#include <ctype.h>
/**
 * _isdigit - check a number through a number
 * @c: var
 * Return: 1 if c is a digit, return 0 otherwise
 */
int _isdigit(int c)
{
if (isdigit(c))
{
return (1);
}
else
{
return (0);
}




return (0);
}
